# 调试步骤

## 问题
前端刷新后只显示1个默认灵感，但后端有3个灵感。

## 调试步骤

### 1. 确认后端正在运行
```bash
# 在 backend 目录运行
python app.py
```

### 2. 测试后端API
```bash
# 在另一个终端运行
python backend/test_get_ideas.py
```
应该显示3个灵感 ✅ (已确认工作)

### 3. 确认前端正在运行
```bash
npm run dev
```
应该在 http://localhost:5173 或类似端口

### 4. 打开浏览器控制台
1. 打开前端页面 (http://localhost:5173)
2. 按 F12 打开开发者工具
3. 切换到 Console 标签
4. 刷新页面 (F5)
5. 查看控制台输出

### 5. 查找这些日志
应该看到：
```
🔄 Loading ideas from backend...
📡 Fetching ideas from http://localhost:5000/api/get_all_ideas
📡 Response status: 200
📡 Received data: {ideas: Array(3)}
✅ Loaded 3 ideas from backend: [...]
✅ Ideas state updated
✅ Loading complete
```

### 6. 如果看到错误
- **CORS错误**: 后端CORS配置问题
- **Network错误**: 后端未运行或端口不对
- **404错误**: API路径错误
- **没有任何日志**: 前端代码未执行

### 7. 使用诊断工具
在浏览器中打开：
```
file:///D:/OneDrive/桌面/IdeaGraph-AI/diagnose-frontend.html
```

这会自动测试：
- 后端健康检查
- 获取所有灵感
- CORS配置

## 可能的问题

### 问题1: 前端未运行
**解决**: 运行 `npm run dev`

### 问题2: 后端未运行
**解决**: 运行 `python backend/app.py`

### 问题3: 端口不匹配
**检查**: 
- 后端应该在 5000 端口
- 前端应该在 5173 或其他Vite默认端口

### 问题4: CORS问题
**检查**: backend/app.py 中的 CORS 配置
```python
CORS(app, resources={r"/api/*": {"origins": "*"}})
```

### 问题5: 浏览器缓存
**解决**: 
- 硬刷新: Ctrl+Shift+R (Windows) 或 Cmd+Shift+R (Mac)
- 或清除浏览器缓存

## 下一步
请执行步骤4-5，然后告诉我控制台显示了什么。
